-content_for :content
  h3 #{@item.title}
  hr
  ul
    li 抓取时间：#{@item.created}
    li
      |来源(本文章来自于互联网，便于资料查阅，转载敬请指明原始出处。)：
      a href="#{@item.link}" target="_blank" #{@item.site.name}
  p
  |#{{@item.content}}
  p
  h4 相关文章
  hr
  -@items.each do |i|
    -unless @item.id == i.id
      blockquote
        a href="/rss/item/#{i.id}" #{i.title}