-content_for :content
  h3 #{@item.title}
  hr
  ul
    li 抓取时间：#{@item.created}
    li
      |本文章来自于互联网，本站收录便于资料查阅，转载敬请指明原始来源：
      a href="#{@item.link}" target="_blank" #{@item.site.name}
  p
  |#{{@item.content}}
  p
  h4 相关文章
  hr
  -@items.each do |i|
    -unless @item.id == i.id
      blockquote
        a href="/rss/items/#{i.id}" #{i.title}